{% extends 'base.html.twig' %}

{% block title %}Editar imagem da notícia{% endblock %}

{% block body %}
    <h1>{{ news.date|date('d/m/Y') }} - {{ news.title }}</h1>
    <h4>Editar imagem da notícia</h4>

    {{ include('admin/news_image/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary float-start ms-3 text-white" href="{{ path('app_admin_news_image_index',{'newsId':news.id}) }}">voltar</a>

    {{ include('admin/news_image/_delete_form.html.twig') }}
{% endblock %}
