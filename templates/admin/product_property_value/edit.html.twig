{% extends 'base.html.twig' %}

{% block title %}Editar valor da propriedade do produto{% endblock %}

{% block body %}
    <h1>{{ product.title }}</h1>
    <h4>Editar Propriedades do produto</h4>

    {{ include('admin/product_property_value/_form.html.twig', {'button_label': 'Update'}) }}

    <a class="btn btn-secondary float-start ms-3 text-white" href="{{ path('app_admin_product_property_value_index',{'productId':product.id}) }}">voltar</a>

    {{ include('admin/product_property_value/_delete_form.html.twig') }}
{% endblock %}
